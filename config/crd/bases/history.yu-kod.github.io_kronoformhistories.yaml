---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: kronoformhistories.history.yu-kod.github.io
spec:
  group: history.yu-kod.github.io
  names:
    kind: KronoformHistory
    listKind: KronoformHistoryList
    plural: kronoformhistories
    singular: kronoformhistory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.snapshotRef
      name: Snapshot
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .spec.appliedBy
      name: Applied By
      type: string
    - jsonPath: .spec.resourceTypes
      name: Resource Types
      type: string
    - jsonPath: .status.appliedAt
      name: Applied At
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KronoformHistory is the Schema for tracking the history of applied
          manifests
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KronoformHistorySpec defines the desired state of KronoformHistory
            properties:
              appliedBy:
                description: AppliedBy indicates who/what applied the manifests
                type: string
              description:
                description: Description provides a human-readable description
                type: string
              manifests:
                description: Manifests contains the original YAML manifests that were
                  applied
                type: string
              resourceNames:
                description: ResourceNames contains the list of resource names affected
                  (e.g., ["my-configmap", "my-deployment"])
                items:
                  type: string
                type: array
              resourceNamespaces:
                description: ResourceNamespaces contains the list of namespaces affected
                items:
                  type: string
                type: array
              resourceTypes:
                description: ResourceTypes contains the list of resource types affected
                  (e.g., ["ConfigMap", "Deployment"])
                items:
                  type: string
                type: array
              snapshotRef:
                description: SnapshotRef references the KronoformSnapshot that created
                  this history
                type: string
            required:
            - manifests
            - snapshotRef
            type: object
          status:
            description: KronoformHistoryStatus defines the observed state of KronoformHistory
            properties:
              appliedAt:
                description: AppliedAt indicates when the manifests were applied
                format: date-time
                type: string
              resourceSnapshots:
                description: ResourceSnapshots contains the before/after states of
                  affected resources
                items:
                  description: ResourceSnapshot represents the state of a single Kubernetes
                    resource
                  properties:
                    after:
                      description: After contains the resource state after applying
                        manifests
                      type: string
                    apiVersion:
                      description: APIVersion of the resource
                      type: string
                    before:
                      description: Before contains the resource state before applying
                        manifests
                      type: string
                    kind:
                      description: Kind of the resource
                      type: string
                    name:
                      description: Name of the resource
                      type: string
                    namespace:
                      description: Namespace of the resource (empty for cluster-scoped
                        resources)
                      type: string
                    operation:
                      description: Operation indicates what operation was performed
                        (Created, Updated, Deleted, Unchanged)
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              summary:
                description: Summary provides a high-level summary of changes
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
